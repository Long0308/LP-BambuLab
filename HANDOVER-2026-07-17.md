# BÀN GIAO — LP-BambuLab hub · 2026-07-16 → 17

> Baseline: `main` @ `a84abb3`, cây làm việc **sạch**, đã push hết. 46 commit trong phiên.
> Mọi số trong tài liệu này là **đo thật** (slice thật / API thật / máy thật), không ước lượng.
> Secrets **chỉ** nằm trong `.env` (gitignore) — đã quét git, không có token nào lọt repo.

---

## 1. TÌNH TRẠNG HỆ THỐNG

| Thành phần | Trạng thái | Ghi chú |
|---|---|---|
| Hub web | ✅ chạy | `https://administrator.tail2d2fb4.ts.net/` (Tailscale) + LAN `:8787` |
| Camera A1 | ✅ live | cổng 6000, không cần mua camera rời |
| Bot Telegram | ✅ 2 chiều | `LPHome_A1_2026_bot` — nút nhanh + AI + điều khiển |
| AI hỏi đáp | ✅ DeepSeek v4 Flash | ~$0.00044/câu · fallback free → paid |
| AI vision | ✅ gemini-2.5-flash-lite | ~$0.0013/lần soi |
| Số dư OpenRouter | $2.96 | hub mới tiêu $0.0053 · `/usage` xem số sống |
| Test | 112/112 pass | `npm test` |

**Khởi động lại hub**: `Start-BambuHub.bat` (tự chặn 2 instance). Log: `server.log`,
nhật ký gửi tin: `notify.log`.

---

## 2. ĐÃ LÀM TRONG PHIÊN (theo thứ tự user chốt)

### 2.1 Ngân sách thời gian slice — bằng số thật (T1)
- **Chốt nghĩa**: thời gian in so với default `0.20mm Standard @BBL A1` — **mục tiêu
  +1h30, sai số tới +2h** ("đừng cố ép"). Vượt mục tiêu mà trong sai số → **giữ chất
  lượng + note tư vấn**, không cắt.
- `optimize_e2e.trim_ladder()` — bậc thang cắt theo **giá đo thật**, re-slice sau mỗi
  bước. **Không bao giờ cắt** lever chống lỗi (warping/kéo sợi/vật cao/brim/support/
  trần mvs).
- **Phát hiện lật ngược TODO cũ**: `enable_support` bật = **+0 giây** (không phải thủ
  phạm #1). Thủ phạm thật: tường 3 (+32m) · trần mvs (+23m) · accel vật cao (+24m).
- Công cụ mới: `bench_ab.py` (A/B từng lever, `--plate N`), `gold_run.py` (chạy cả thư mục).
- **Gold set 83 file Downloads**: lọt mục tiêu +1h30 — Nhanh 75/76 · Cân bằng 70/76 ·
  Đẹp 60/76. Vượt +2h chỉ 3 ca. 4 file lỗi là lỗi **của file** (plate rỗng / to hơn bàn).

### 2.2 Bug + audit dữ liệu
| Việc | Root cause thật |
|---|---|
| **Cân nặng đa màu** | Bambu ghi `106.56,12.77` trên 1 dòng — regex dừng ở phẩy → thiếu 11%. Fix 3 chỗ. |
| **Generic PLA** | `"PLA" in "GENERIC PLA"` → rơi vào họ PLA. Thêm entry riêng, xác minh 2 nguồn. |
| **PLA Lite 210→220** | Bambu có **2 tầng profile**: `@base` 210 nhưng `@BBL A1` **override 220**. Phiên trước chỉ tra @base. |
| **Audit 13 nhựa** | 6 lỗi: ASA inherits **404** (import fail!) · PETG HF dính đúng bug 2 tầng · ABS mvs 29→16 · TPU sai 3 tầng · SILK/CF mvs nhanh hơn official · họ PLA bàn 55→65. |
| **Code-reviewer** | 10 findings (3 HIGH) — xử lý hết. |

### 2.3 Camera + thông báo + AI
- `camera_stream.py`: giao thức cổng 6000 (TLS + `bblp` + Access Code). 1 thread bơm,
  n viewer = 1 kết nối, tự ngắt sau 15s. Web dùng **double-buffer poll 1.2s** (chống xé hình).
- `notify.py`: ntfy / Telegram / Discord qua `.env` (sửa **không cần restart**).
  Mốc **30/50/75/100%** · lỗi = **báo động 10 tin** + ảnh + link · retry ×3 (mạng VN bóp Telegram).
- `telegram_bot.py`: nút nhanh, AI vision, **điều khiển** (Dừng hẳn phải gõ `DUNG XAC NHAN`
  trong 60s). Chỉ trả lời `TELEGRAM_CHAT_ID` — người lạ im lặng tuyệt đối.
- `ai_chat.py`: prompt kiểu **ECC** (vai trò → máy → sự thật → 6 luật trả lời) +
  `_knowledge()` **rút thẳng từ code** 7 mục A–G (số nhựa, đặc tính, vật cao, vị trí 50
  option Studio, ngưỡng hình học, **sự cố→sửa**, mã HMS) → sửa `analyzer.py` là prompt tự đổi.
- `ui_tg.py`: **1 nguồn UI** cho mọi tin — thẻ bố cục như Bambu Studio (tên file trên đầu →
  bar + % → lớp/còn lại → **giờ xong dự kiến** → link). Mốc tiến độ dùng **chung hàm** với
  "Tình hình in" nên không thể lệch.

### 2.4 Các fix bắt được từ ca chạy thật
| Hiện tượng | Nguyên nhân thật | Fix |
|---|---|---|
| AI báo "lệch trục" ở 65% | Chụp trúng lúc bàn phóng | Loạt 3 frame + luật "nghiêng phải nhất quán mọi ảnh" |
| Ảnh 75% méo mó | **In đêm, đèn tắt** → camera phơi sáng dài (ảnh 20:39 ban ngày đang in vẫn nét) | `cmd_light()` **bật đèn máy trước khi chụp**, trả lại trạng thái cũ (MQTT `ledctrl`) |
| Verdict báo HỎNG ngược | Câu "**KHÔNG** có dấu hiệu **HỎNG**" chứa chữ "HONG" | Chỉ đọc nhãn sau `KQ:`, bỏ dấu — 9/9 case đúng |
| Tin mốc lặp | Restart giữa chừng bắn lại mốc cũ | Mốc đã qua tự đánh dấu im lặng |
| Ảnh hero sai khay | Sort alphabet lấy `plate_1` | Ưu tiên `plate_N.png` **có** `plate_N.gcode` |
| Kẹt Matte đen (thật) | Mã `1200-8007` | Web hiện mã hex + **nút Tiếp tục** trong banner; Telegram dịch tiếng Việt + nhắc Resume |

---

## 3. VIỆC PHIÊN SAU

1. **Đêm nay kiểm chứng fix đèn**: mốc 70%/90% khi in đêm — xem ảnh AI soi còn nhoè
   không (`notify.log` ghi verdict). Nếu vẫn nhoè → cân nhắc chụp lúc `gcode_state`
   đổi lớp, hoặc dùng Timelapse truyền thống (bàn về vị trí park mỗi lớp — tốn thời gian in).
2. **Điều tra `AMS+Lite+Top+Mount-Final.3mf`** — ca duy nhất vượt +2h ở **cả 3 mode**
   (Nhanh +6h50m với **0 bước cắt**). Nghi trần mvs theo nhựa khai báo trong file đội
   thời gian. Soi `slice_jobs/gold/gold_results.jsonl` trước khi sửa gì.
3. **Nghịch lý Cân bằng > Đẹp** (Notebook_stand: 13h30 vs 12h05) — do Đẹp bị cắt tường
   còn Cân bằng giữ. Cân nhắc note giải thích hoặc đồng bộ bậc cắt.
4. Cân nhắc `/revoke` token Telegram + tạo lại OpenRouter key (đã dán trong chat).
5. Ý tưởng: chỉ cap tốc theo trần mvs khi **khay AMS thật** là Matte/Metal, thay vì theo
   khai báo file.

---

## 4. GROUND TRUTH (đừng tra lại)

- **BUCKET khay 1**: `total_predication` **12174s = 3h22m54s** (= số GUI); header "model
  printing time" 3h16m36s là thời gian in thuần. Cân nặng 106.56 + 12.77 = **119.33g**.
- **Giá lever** (giây, so với 12174): layer016 +2734 · walls3 +1927 · tall3000 +1449 ·
  mvs138 +1407 · ironing +1168 · tall4000 +947 · infill12 +556 · outer150 +424 ·
  shells64 +367 · arachne +288 (−5.9g) · **support +0** · layer028 −1864.
- **Tên inherits đã xác minh**: `Bambu PLA Lite/Matte/Metal @BBL A1` · `Bambu PETG Basic
  @BBL A1` · `Generic PLA @BBL A1` · `Bambu ASA @BBL A1 0.4 nozzle` (bản không-nozzle **không tồn tại**).
- **Bambu 2 tầng profile**: luôn tra **cả** `@base` **và** `@BBL A1` — tầng máy override tầng base.
- **CLI quirk**: treo ở bước thoát sau khi đã ghi xong kết quả (hay gặp khi Studio GUI mở)
  → log ra file + poll + kill cây process; crash ngẫu nhiên → retry.
- **Không chạy 2 process CLI song song** (`CLI_LOCK` chỉ chống trong 1 process) — 22 file
  từng fail `return_code=None` vì lý do này.
- **Vision model**: `gemini-2.5-flash-lite` thắng đấu loại 8 model trên frame thật (duy
  nhất mô tả đúng vật thể). **DeepSeek không có vision** và **không có bản free** (tra 11 model).

---

## 5. CẤU HÌNH (.env — không commit)

```
BAMBU_HOST / BAMBU_SERIAL / BAMBU_ACCESS_CODE     # máy in
TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID             # bot 2 chiều + chuông
HUB_URL=https://administrator.tail2d2fb4.ts.net/  # link đính kèm tin báo
OPENROUTER_API_KEY
OPENROUTER_MODEL=deepseek/deepseek-v4-flash       # chat
OPENROUTER_VISION_MODEL=google/gemini-2.5-flash-lite
OPENROUTER_PAID_MODEL=openai/gpt-5-nano           # cuối chuỗi fallback
```
Khuôn mẫu đầy đủ + hướng dẫn từng bước: `.env.example` và `README.md`.